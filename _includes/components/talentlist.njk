<section>
  <div class="talent-grid">
    {% for post in projectlist | reverse %}
      <div class="talent-card" {% if post.url == url %} data-current="current item"{% endif %}>
        <h4>
          <a class="talent-card__title" href="{{ post.url | url }}">
            {% if post.data.title %}
              <span class="talent-card__emoji">{{ post.data.emoji }}</span>
            {% endif %}
            {% if post.data.title %}
              {{ post.data.title }}
            {% else %}
              Untitled
            {% endif %}
          </a>
        </h4>
        {% if post.data.summary %}
          <p>
            {{ post.data.summary }}
          </p>
        {% endif %}
        {% if post.data.tags %}
          <p class="tag-list">
            {% for tag in post.data.tags %}
              {%- if tag != "talent" -%}
                {% set tagUrl %}/talent-tags/{{ tag }}/{% endset %}
              <a class="talent-card__tag tag" href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
              {%- endif -%}
            {% endfor %}
          </p>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</section>
